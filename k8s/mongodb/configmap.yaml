apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-configmap
  namespace: mongodb
data:
  # MongoDB service ka DNS name
  database-host: "mongodb-service.mongodb.svc.cluster.local"
  database-port: "27017"
